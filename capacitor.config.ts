import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "pl.devgrupa.countanything",
  appName: "Count Anything",
  webDir: "dist",
};

export default config;
